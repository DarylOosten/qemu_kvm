---
packages: {
  Debian: [
    bridge-utils,
    qemu-system,
    qemu-utils,
    libguestfs-tools,
    libvirt-clients,
    libvirt-daemon-system
  ],
  LMDE: [
    bridge-utils,
    qemu-system,
    qemu-utils,
    libguestfs-tools,
    libvirt-clients,
    libvirt-daemon-system
  ],
  Ubuntu: [
    bridge-utils,
    qemu,
    qemu-kvm,
    libguestfs-tools,
    libvirt-clients,
    libvirt-daemon-system
  ]
}

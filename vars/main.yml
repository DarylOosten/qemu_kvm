---
packages: {
  Debian: [
    bridge-utils,
    qemu-system,
    qemu-utils,
    libguestfs-tools,
    libvirt-clients,
    libvirt-daemon-system,
    virtinst,
    virt-manager
  ],
  LMDE: [
    bridge-utils,
    qemu-system,
    qemu-utils,
    libguestfs-tools,
    libvirt-clients,
    libvirt-daemon-system,
    virtinst,
    virt-manager,

    spice-client-gtk
  ],
  Ubuntu: [
    bridge-utils,
    qemu,
    qemu-kvm,
    libguestfs-tools,
    libvirt-clients,
    libvirt-daemon-system,
    virtinst,
    virt-manager
  ]
}
